cert = /etc/stunnel/private.pem
pid = /var/run/stunnel.pid
client = no
[redis]
accept = 127.0.0.1:6379
{% for host in groups['database'] %}
connect = {{ hostvars[host]['ansible_ssh_host'] }}:6379
{% endfor %}
